dartdoc:
  # These libraries only re-export `package:hashlib` and `package:convertlib`
  # for single-import convenience. Their canonical documentation lives in those
  # packages.
  exclude:
    - 'package:cipherlib/hashlib.dart'
    - 'package:cipherlib/codecs.dart'
